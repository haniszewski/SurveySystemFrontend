type User = {
  email: string;
  username: string;
  token: string;
};

type CreateUser = {
  email: string;
  username: string;
  password: string;
};

type LoginUser = {
  email: string;
  password: string;
};
