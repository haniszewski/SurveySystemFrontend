import AnalysisSchemaStart from "../molecules/analysis/analysis-schema-start";

const UpdateAnalysis = ({ survey }: { survey: Survey }) => {
  return <AnalysisSchemaStart />;
};

export default UpdateAnalysis;
